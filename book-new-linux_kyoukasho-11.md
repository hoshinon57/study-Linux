## 新しいLinuxの教科書 読書メモ 11章～

### Chapter11 標準入出力とパイプライン
#### 標準入力、標準出力、標準エラー出力
標準エラー出力: プログラムのエラーメッセージを出力するための標準的な出力。通常は標準出力と同じく端末ディスプレイ。  

#### リダイレクト
<: 標準入力を切り替える  
cat /etc/crontab  
cat < /etc/crontab  
は得られる結果は同じだが、コマンド内部での動きは結構違うとのこと。  

Linuxで何らかのコマンドを作るときは、他のLinuxコマンドと同様に、
「ファイルを指定する」ではなく「標準入力から受け取る」  
仕組みで作る方がよい。  

\>: 標準出力を切り替える  

標準エラー出力: 主にプログラムのエラーメッセージを出力するために使われる  
ls /(存在しないフォルダ)  
でエラーメッセージが表示されるが、これは標準エラー出力に出力される。  
そのため  
ls /(存在しないフォルダ)  > out.txt  
ではout.txtにエラーメッセージは出力されない。  

2\>: 標準エラー出力を切り替える  
2\>&1: 標準エラー出力と標準出力をまとめる  
ls . /xxx > out.txt 2>&1  
ls . /xxx 2>&1 > out.txt 　　**※これだと上手くいかなかった。2番と1番をまとめるが、次に1番だけout.txt宛てに変更するから、、という感じだろうか。**  

\> は既存のファイルがあるときは上書きされてしまう。  
\>\> にすると、ファイル末尾に追記する。  
set -o noclobber にて、既存のファイルを上書きしてしまうときにエラーにできる。  

/dev/null  
入力先として指定：何も内容を返さない  
出力先として指定：出力したデータは保存されず消える  
標準出力を消したい(エラーのみ表示したい)とか、標準エラー出力を消したい(エラーが出ることが分かっている)とか、に使える。  
\>/dev/null 2>&1 で、標準出力＆標準エラー出力をともに捨てる  

#### パイプライン
1つ目のコマンドの標準出力が、2つ目のコマンドの標準入力に送り込まれる  
標準エラー出力も送るには、 2>\&1 を使う  

#### フィルタ
標準入力を入力として受け取り、標準出力に出力するコマンド、  
このようなものをフィルタと呼ぶ  

head:  
ファイルの先頭から指定した行数だけ標準出力に出力  
指定無ければ先頭10行  
-n: 行数指定  

du:  
ファイルやディレクトリの使用容量を表示する  
du [オプション] [ファイル/ディレクトリ]  
-b: バイト単位で表示  

**シンプルなコマンドを必要に応じて組み合わせる方が複雑なことを実現しやすく、また柔軟性もある。**  

---
### Chapter12 テキスト処理
#### wc
バイト数・単語数・行数をカウント  
\$wc /etc/passwd  
21 31 973 /etc/passwd  
行数、単語数、バイト数 の順  
-l, -w, -c: それぞれ 行数、単語数、バイト数 を表示  
標準入力を明示したいときは、入力ファイルとして - を指定する  

#### sort
sort file1: アルファベット順(ASCII文字順)にソート この時、末尾に空行があるとその行もソート対象になる  
-k (数値): そのフィールドでソート  
-n: 数値順にソート  
-r: 逆順にソート  
-u: 重複行を取り除く(uniqコマンドを使わずに実現できる)  
-t (区切り文字): 指定した文字で区切る  

#### uniq
重複行を取り除く  
**同じ行が連続している場合のみ取り除く点に注意** そのため通常はsortコマンドとセットで使う  
-c: 重複している行数を表示 ⇒ 各データの件数が分かる  
　⇒さらにsortコマンドに渡すことで、件数の多い／少ない順に表示できる  

#### cut
入力の一部を切り出す  
cut -d <区切り文字> -f <フィールド番号> [<ファイル名>]  
　⇒cut -d , -f 3 file.csv: カンマを区切り文字として3番目のフィールドのみを表示する  
-dオプション無しのときは、タブが区切り文字になる  
**区切り文字、なので指定できるのは1文字だけみたい**  
-f 1,5,6 のようにして複数フィールドを表示できる(この場合は1,5,6番目のフィールド)  

#### tr
文字を変換・削除する  
**純粋なフィルタとして設計されており、ファイルから直接読み込むことはできない**  
tr <置換前の文字> <置換後の文字>  
例）
tr : , ⇒ :を,に置き換える  
tr abc ABC ⇒ aをA、bをB、cをC  
tr a-z A-Z ⇒ 小文字を大文字  
※文字列の置換はsedやawkを使う  

tr -d <削除文字>: 文字を削除  
例）
tr -d "\n": 改行コードを削除する  
tr -d a-z: a～zを削除できた  

-s: 指定した文字が連続する場合、1文字に置き換える

#### tail
末尾部分を表示  
オプション無しでは末尾10行が対象  
-n (行数): 行数指定 -(行数)も同じ  
-c (バイト数): バイト数指定  

ファイルへの追記を監視することができる。ログの追加など。  
tail -f <ファイル名>  

#### diff
diff [オプション] <変更元ファイル> <変更後ファイル>  
<範囲1>acd<範囲2>  
a: 1の後に2が追加  
c: 1が2に変更  
d: 1が削除  
範囲1は変更元ファイルを、範囲2は変更後ファイルを指す  
<: 1つ目のファイルだけにある行  
\>: 2つ目のファイルだけにある行  

-u: ユニファイド形式  
変更のあった行だけでなく、その前後何行かも表示してくれる  
例）  
@@ -14,3 +14,5 @@　←ここからかたまり  
-14,3: 1つ目のファイルの14行目から3行 ※変更が無い行も含めて  
+14,5: 2つ目のファイルの14行目から5行  

ファイルを変更したとき、変更後のファイル全体を送るのではなく、diffコマンドの出力結果を送ることもできる。  
元ファイルと差分ファイルから、変更後ファイルを復元できるため。  
こういった差分ファイルをパッチと呼び、diffコマンドの出力はpatchコマンドで適用できる。  

---
### Chapter13 正規表現
#### grepコマンドと正規表現
grep [オプション] <検索パターン> <ファイル名>  
grep bash /etc/passwd: "bash"を含む行を出力  
ファイル名を省略すると、標準入力から読み込む  

-n: マッチした行の行番号を表示  
-i: 大文字小文字を区別しない  
-v: マッチしなかった行を表示  

正規表現：  
^: 行の先頭  
正規表現では * { } $ などの記号が特別な意味を持つ(メタ文字)。メタ文字の多くはシェルにとってもまた別の意味がある。  
シェル展開を防ぐため、 '' で囲むとよい。  
この目的のため、''で囲む必要が無いケースももちろんあるが、常に '' で囲んで問題ないので、それを推奨。  

#### 文字にマッチするメタ文字
.: 任意の1文字  
\\: メタ文字の前に置くことで、そのメタ文字の意味を打ち消す(エスケープする)  
⇒ \\. は . そのものになる  

\[abc\]: カッコ内のいずれかに一致  
\[abcdefghijk\] は \[a-k\] で良い  
\[^13\]: 1,3以外 カッコ内の先頭に ^ を置くことで、それ以外を示す  

#### 位置にマッチするメタ文字
^: 行の先頭  
⇒ '^net' で行頭に net  
\$: 行末  
^\$: 行頭の直後が行末、つまり空行にマッチする。-vと組み合わせることで空行を除いて表示できる    

#### 繰り返しを指定するメタ文字
直前の正規表現が一定回数繰り返されることを意味する  
*: 0回以上の繰り返し  
⇒ 'Be*r' だと Br にもヒットする  
　\[ea\]* で \[ea\]が0回以上繰り返される文字列にマッチ  
　.* で任意の文字の0回以上の繰り返し、つまりあらゆる文字列にマッチ  
　⇒ '^ex.*txt$' で、「exで始まってtxtで終わる行」  

※この段落はすべて拡張正規表現の記法  
基本正規表現と拡張正規表現：  
-E: 拡張正規表現として解釈される  
-Eオプションをつけなくても、メタ文字の前に \ をつけることで拡張正規表現のメタ文字を使えるが、混乱しやすいので非推奨。  
+: 直前の文字の、1回以上の繰り返し  
?: 直前の文字の、0回または1回の繰り返し  
{m,n}: m回以上n回以下の繰り返し  
{m}: ちょうどm回  
　⇒ \[0-9\]{3}-\[0-9\]{4} で「3桁-4桁」、つまり郵便番号にマッチする  
{m,}: m回以上  

#### その他のメタ文字
※この段落はすべて拡張正規表現の記法  
(): グループ化
　⇒ '(Wine){2,}'  で"Wine"2回以上にマッチ  
|: OR条件  
　⇒ '(Vodka)|(Wine)' で "Vodka"か"Wine"にマッチ  

---
### Chapter14 高度なテキスト処理
