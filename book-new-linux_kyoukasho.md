## 新しいLinuxの教科書 読書メモ

### Chapter01
CentOSのISOイメージ、本では4GBとあったが最新(ver10, ver9)は10GBほど必要になり「うーん」だったので、古いバージョンをダウンロードすることとした。  
https://www.centos.org/download/ の Older Versions から https://vault.centos.org/ へ移動。  
archive.kernel.org の isos フォルダから CentOS-7-x86_64-DVD-2009.iso をダウンロードした。

VirtualBoxの仮想マシンのウィンドウをクリックすると、仮想マシン内のOSにマウスとキーボードの制御が奪われる。  
解除するにはホストキーの入力が必要。Windowsであれば右Ctrlキー。

CentOSでは、 su でrootユーザーになる。  
※もしくは、ログイン時にrootでログインする。  
　Ubuntuではrootログインは不可能。sudoを使う。

shutdown: シャットダウンコマンド  
スーパーユーザでのみ実行可能。  
-h now: 即時電源断  
-r now: 即時再起動  

---
### Chapter02
ある作業を行いたいときにぴったりのコマンドがなければ、コマンドをいくつか組み合わせて処理をするというのがLinuxのやり方。  
「1つのプログラムには、1つのことをうまくやらせる」  
1つ1つは単純なコマンドでも、組み合わせることで複雑な仕事をこなすことができる。

シェル：カーネルのインターフェースとなっているソフトウェア  

プロンプトは一般ユーザーは$で、スーパーユーザは#で表現される（ことが多い）。

ログインシェル：ログイン時に最初に起動されるシェル  
$ echo $SHELL  
で確認できる。  
Linuxのデフォルトのログインシェルは bash .  
logoutコマンドで終了できるのはログインシェルのみ。exitで終了する。

他のシェルに一時的に切り替えたい場合、単に  
\$ sh  
などすればOK. 多重起動しているイメージ。  

ターミナル：コンピュータへ入出力する際に利用する専用のハードウェア  
現在はハードウェアとしてのターミナルが使われるケースはほとんど無く、ソフトウェアで実装したターミナルエミュレータが利用される（現在はターミナルと言えばこれを指すことが多い）。Linuxマシン上で直接動かしたり、Windows上で起動してLinuxマシンを操作したり。  
Windowsの主なターミナルエミュレータ：PuTTY, Tera Term  

**ターミナルとシェルは全くの別物。**  
ターミナルエミュレータは、入出力の画面を提供するだけのもの。  

---
### Chapter03

ショートカットキー
^b: 1文字後ろ(最初に入力する側)  
^f: 1文字前  
^a, ^e: 先頭、行末  
Meta+b, Meta+f: 後方、前方の単語1つ分 (Metaは左側のAltキーで代用)  
^h: Backspace  
^d: Delele **ただしコマンドラインに何も入力してない状態だとログアウト操作になってしまう点に注意**  
^w: 後方にスペース区切りで1単語削除  
^k: カーソル位置から行末までを削除(カット)  
^u: カーソル位置から行頭までを削除(カット)  
^y: ヤンク(ペースト)  
^s, ^q: 画面表示のロックと解除 ただし自分の環境では動作せず(^sを押しても以降の操作を変わらずできた)  
^c: 実行中のコマンドを強制終了 コマンドラインにて入力内容を破棄するときにも使う  
^l: 画面クリア clearコマンドも同様  

reset: 端末を初期化するコマンド。^lだと上にスクロールすると消去前の情報が表示されるが、resetでそれも消せる  

Tabでの補完機能は、シェルによるもの。  

^p, ^n: コマンド履歴  
^r: コマンド履歴から検索(インクリメンタル検索)  
⇒ここから文字を入力して検索、^rで1つ前の検索結果へ、Enterでそれを実行、Escで検索結果を表示したままプロンプトへ、^gで破棄

man ascii: ASCIIコードの一覧を表示  
コンピュータが扱う数値(バイト列)について、文字をどの数値に割り当てるかを決めたのが文字コード。  
Linuxで日本語を扱うには、  
以前：EUC-JP  
現在：UTF-8 が主流  
現在の設定は、printenvで LANG の値で確認できる  
ファイルの文字コードを変換するには iconv というコマンドを使う(試していない)  

---
### Chapter04

Linuxではすべてがファイルとして表現されている。  
HDDやキーボードといった入出力装置も。  

ディレクトリの区切り文字は、Linuxでは / を用いる。(キーボード右下にある記号)  
Windowsは \ を用いる。(キーボード右上)  

Windowsではc:\, d:\ のようになるが、Linuxでは複数ディスクがあってもシステム全体でディレクトリツリーは1つ。  
例えば2台目のディスクは /var とかにマウントされる。  

/bin: 一般ユーザおよび管理者ユーザの両方が利用するコマンドの、実行ファイル置き場  
/dev: デバイスファイル置き場  
/etc: 設定ファイル置き場 Linux自体の設定にかかわるファイルも置かれる  
/home: ユーザごとに割り当てられるホームディレクトリ  
/sbin: /binの管理者ユーザー向け  
/usr: 各種アプリケーションと付随するファイル置き場  
/var: アプリケーションのデータやログ、電子メールなど 容量がかさむことが多い  

pwd: カレントディレクトリを表示

絶対パス: /usr/bin/perl のようにルートディレクトリを起点にパスを示す書き方。フルパスとも呼ぶ  
相対パス: 現在のカレントディレクトリを起点  
同じフォルダに file1 というファイルがあるとき、file1でもよいのだが、これだと単にファイル名を書いているのかパス表記なのかが分からない。  
./file1 と書くことでパス指定を明示できる。  

cd -P /usr/local  
-Pがオプション  
"-P /usr/local" がコマンドライン引数  
Linuxのコマンドは、成功時には何も表示しないのが普通  

cd ~ でホームディレクトリへ移動する。~はシェルにてホームディレクトリのパスに置き換えられる(チルダ展開)。  
cd でも同じ。

パス名展開 シェルの機能  
*: 任意の文字列  
?: 任意の1文字  

lsコマンド  
-l: ファイルの詳細情報を表示する  
　ファイルタイプは、-が通常ファイル、dがディレクトリ、lがシンボリックリンク  
-a: 隠しファイルも表示
　lsコマンドは . から始まるファイルを隠しファイルとみなして表示しない  
-F: ファイルの種類を表す記号を表示する  
-w: -w 30 で30桁指定で表示  

コマンドのオプションは大体のコマンドで同じ規則。  
ls -a -F は ls -aF でOK.  
コマンドの引数とオプションを同時に指定する場合、先にオプションを書く。  
⇒ls -aF /  
ls -w 30 の30はコマンドへの引数ではなく、オプションの引数。  
またこの場合、オプションと引数間のスペースは不要。 ls -w30 のように書ける。  

ロングオプション：  
--... とハイフン2つで始まるオプションを指す。 (e.g. ls --quote-name)  
ロングオプションは一意に示せるならば、後半を省略できる。 (e.g. ls --quote でも可)  
ロングオプションの引数は、スペースもしくは = を使う。 (e.g. ls --width=30)  

---
### Chapter05
Linuxでは既存のファイルと同名のディレクトリを作ることはできない。  
ディレクトリはファイルの一種として扱われているため。

mkdir: 
mkdir report/2026/09  
としたときに途中のディレクトリが存在しない場合はエラーになる。  
一気に作りたいときは -p オプションにて。

touch: ファイルを作成する  
本来はファイルのタイムスタンプを更新するコマンド。ファイルが存在しない場合は新規作成する仕様のため、空ファイルを作成する用途にもよく使われる。  
touch (既存ファイル名) としても中身が消えることはないので安心。  

rm: ファイル削除  
rm file1 file2 file3 で複数削除できる。  
削除確認はしない。  
-r: ディレクトリを削除したい場合は、再帰的に削除するオプション -r を使う。  
　⇒ rm -r (ディレクトリ名)
-i: 削除確認する

rmdir: ディレクトリ削除  
中にファイルがあるとエラーになる。これはrmとの大きな違い。  
'.'から始まる隠しファイルを見落としていた場合も安心。  

cat: ファイルの中身を表示  
複数ファイルを指定すると、ファイルを連結して表示する。  
conCATenate(連結する)の略らしい。  
-n: 行番号を付けて表示  
ファイル名を指定せずcatコマンドを実行すると、キーボードからの入力を待ち、それをそのまま表示する動作になる。^dか^cで終了。  
他にも「入力ファイルを指定しないとキーボードからの入力を受け付ける」というコマンドはLinuxに多いとのこと。  

less: ファイルの中身を表示 1画面ごと  
スペース, f: 1画面下にスクロール  
b: 1画面上にスクロール  
j, k: 1行スクロール  
/: 検索へ移行する。nで次の検索結果、Nで前の検索結果へ。  

cp: ファイル、ディレクトリをコピー  
既存ファイルへのコピー上書き確認はしない。  
cp file1 file2: file1をfile2という名前でコピー  
cp file1 dir1: file1をdir1の中へコピー  
cp file1 file2 file3 dir1: ディレクトリへコピーする場合、複数ファイル指定が可能  
-i: 上書き確認する  
-r: ディレクトリをコピーする際は、再帰的に行う -r オプションをつける  
　⇒ cp -r dir1 dir2: dir1をdir2の中にコピーする(./dir2/dir1 というディレクトリができる)  

mv: ファイルを移動  
mv file1 file2: ファイル名の変更と同じ  
mv file1 dir1: dir1へ移動  
mv dir1 dir2: dir1をdir2の配下に移動 cpと違って-rオプションは不要  

ln: リンクを張る  
- ハードリンク:  
1つのファイルに複数の名前をつける機能。最近はあまり使われない。  
ln file1 file2: ハードリンクfile2を作成  
ハードリンクの実体は、すべてのハードリンクが無くなったときに削除される。  
ディレクトリに対して作成できない、異なるディスク間をまたぐことができない、といった制約がある。  
- シンボリックリンク:  
-s で作成する。  
リンク先が書かれたファイル、のイメージ。  
シンボリックリンクを残したまま、リンク先の実体ファイルを削除することもでき、「リンクが壊れた」状態となる。  
※本では「深いディレクトリ先に張る」「複数バージョンを共存させ、使う方に張る」といった例が挙げられている。  

---
### Chapter06

find: ファイルを探す  
find <探索開始ディレクトリ> <検索条件> <アクション>
find . -name '*.txt' -print  
*や?を使う場合、シェルによるパス名展開が行われないよう、'', ""で囲むのが良い。  
find . -type d -print
f: ファイル、d: ディレクトリ、l: シンボリックリンク  
-a で区切ることでAND条件  

locate: データベースからファイルを探す  
自分の環境では、最初からインストールはされていたが、updatedbコマンドの初回実施が必要だった。  
updatedbはrootユーザで行う。  
locate '*.sed'  拡張子が.sedのファイルを探す

--helpオプション: 多くのコマンドで使用可能で、コマンドのヘルプメッセージを表示する。  
man: コマンドのオンラインマニュアル(※)を表示する。  
　(※)電子化されたマニュアル、の意。  
　man -k (キーワード): キーワードからマニュアルを探す。  
　自身の環境では、最初にrootユーザで mandb を行う必要があった。  
　man (セクション番号) (コマンド名): 例えばcrontabはセクション1,5に存在する(内容は異なる)。  
　セクション番号を省略すると、若い番号の方が表示されるため、番号指定が必要になるケースもある。 
  man -wa (コマンド名): セクション番号のリストを表示  

**シェルがコマンドを実行する際に、$PATHという環境変数に設定された場所から自動的にコマンドを探してくれる。**  
which: コマンドのフルパスを表示する
-a: すべてのサーチパスから検索する

日本語を指定してcatのヘルプを表示する  
LANG=ja_JP.UTF-8 cat --help  
英語で表示する  
LANG=C cat --help  (LANG=Cはデフォルトを意味しており、すなわち英語になる)  

---
### Chapter07
Vimはviの後継として開発された。互換性のためviコマンドでもVimを起動できる。  
とりあえず、Escキーを何度か押すと「コマンドを入力するモード」に戻ることができる。  

ファイルの終わり以降を、行頭チルダで表現する。

| コマンド | 内容 |
| --- | --- |
| :q | 終了 |
| :q! | 保存せず終了 |
| :w | 保存 |
| h, l | 左、右 |
| j, k | 下、上 |
| x | カーソル位置の文字の削除 |
| i | 挿入モード(インサートモード) |
| a | 挿入モードだが、カーソルの右側に文字を追加する |
| w, b | 次/前の単語の先頭に移動する(区切りはスペース以外にも , や () も区切りとみなされる) |
| W, B | 次/前の単語の先頭に移動する(区切りはスペースのみ) |
| 0, $ | 行頭、行末へ移動 |
| 5G, :5 | 5行目へ移動 |
| gg, G | 最初の行、最後の行へ移動 |
| J | 下の行と連結 (xでは行末の改行を削除できないため) |
| u | Undo |
| ^r | Redo |

挿入モード時にEscを押すとノーマルモードに戻る  

- デリート(Windowsのカット)  
d0, d$: 行頭まで、行末まで  
x, dl: 1文字  
dw: 単語1つ  
dgg, dG: 最初の行まで、最後の行まで  
dd: 現在の行  
x4: 4文字 (文字数指定)  
※「ここからあの文字までデリートしたい、WindowsのShift+カーソルキーのように」の場合、vでビジュアルモードに入り、h,lなどで選択し、dでデリートするらしい。  

- プット(Windowsのペースト)  
p  
カーソルの右から貼り付けるみたい  

- ヤンク(Windowsのコピー)  
dの代わりにyを使う  
yy: 現在の行  
5yy: 5行 (文字数指定)

- 検索  
/ を押してから検索文字列を入力  
n, Nで次、前の検索結果へ  

- 置換  
: を押してから、以下    
%s/(前)/(後)/g

- ヘルプ  
vimtutor: チュートリアル  
:help : ヘルプ画面  
:help :q : :q に対するヘルプ  

---
### Chapter08

#### エイリアスの設定  
alias ls='ls -alF --color=auto'  
=の前後にスペースは入れてはNG. コマンドと解釈されてしまうため。  
alias rm='rm -i' として削除確認をする使い方も多い  

typeコマンドで、そのコマンドが本当にコマンドなのかエイリアスなのか、確認できる。  
type cp  
type ls  ※自身の環境では、'ls --color=auto'のエイリアスだった  

unalias: エイリアスの削除  
unalias ls  

一時的にエイリアスを無効にしたい場合：  
- /bin/ls のようにフルパスで指定する  
- command を使う。エイリアスではなく明示的にコマンドを実行できる。  
⇒command ls  
- \ls とする。

#### bashのオプション
bashでは、シェル自身が持つさまざまな機能を利用するためにオプションを指定することができる。  
オプションは各機能ごとにオン・オフの2択。  

set:  
set -o <オプション名>: オン  
set +o <オプション名>: オフ  
設定一覧を見る: set -o  
ignoreeof: ^Dを無視する、をオン  
noclobber: 既存ファイルをリダイレクトで上書きしない  
noglob: *などのパス名展開を無効にする  

shopt:  
shopt -s <オプション名>: オン  
shopt -u <オプション名>: オフ  
shopt <オプション名>: オンオフを表示  
設定一覧を見る: shopt  
setとshoptとで使えるオプションは異なる。  
autocd: ディレクトリ名のコマンドを実行すると、それがcdの引数として実行される  
dotglob: パス名展開の結果に、.で始まるファイルを含める  
　⇒通常、 * では . から始まるファイルにはマッチしない  
cdspell: cdコマンドにて、ちょっとしたミスタイプを自動修正してくれる  
globstar: ** を使うと、サブディレクトリまで含めたすべてのファイルにマッチする  

#### シェル変数
シェル変数：bash内部で使用される変数  
var1='test variable'  ★=の左右にはスペースを入れないこと  
echo $var1  で表示  

PS1: プロンプト設定  
\u: 現在のユーザー名  
\w: カレントディレクトリ(フルパス表記っぽい)  
\W: カレントディレクトリ(末尾のディレクトリのみ、っぽい)  
\\$: 一般ユーザは\$, rootユーザは#, になるのかな  
\n: 改行  
\d, \t: 日付, 現在時刻

自分で独自のコマンドを作成したとき、 ~/bin に置くことが多い。  
PATH="$PATH:~/bin"  
とすると、そこを追加できる。  

**上記は ' ではダメ。' は中の文字をすべて「ただの文字列」として扱うため、$PATHの展開がされない。**  
**\" だと中の文字列に含まれる \$ を展開してくれる。**

LANG: ロケール  
locale -a: システムがサポートしているロケールを表示  
日本語は ja_JP.utf8  

その他のシェル変数  
HISTFILE: コマンドライン履歴を保存するファイル名 デフォルトは ~/.bash_history  
HISTFILESIZE: 上記に保存する履歴の最大行数  
HISTSIZE: **現在のシェルで、**保持する履歴数  
HOME: ホームディレクトリ  
SHELL: ログインシェルのパス  
PWD: カレントディレクトリ  

#### 環境変数
外部コマンド: cp, lsなど、実行ファイルとしてファイルシステム上に存在するコマンド  
組み込みコマンド: setなど、シェル内蔵のコマンド  
type <コマンド> で判別できる  

外部コマンドでは、前章のシェル変数を参照することができない。シェルの"外側"で実行されるため。  
外部コマンドからも参照できるようにした変数を、環境変数と呼ぶ。  
例えばLANGは環境変数。(catコマンドはLANGによってメッセージが変わる)  

printenv: 環境変数の表示  

export <シェル変数名>: 指定したシェル変数を環境変数にする  
LESS='--no-init'  
export LESS  
もしくは、  
export LESS='--no-init'  

#### bashの設定ファイル
今後ログインしなおしたときにも同じ設定になるようにするには、bashの設定ファイルに記述する。  
ログインシェルとして起動:  /etc/profile ⇒ ~/.bash_profile ⇒ ~/.bashrc の順で読み込まれる。  
非ログインシェルとして起動: ~/.bashrc が読み込まれる。  

/etc/profile はシステム全体で使用する設定ファイル。  
~/.bash_profile と ~/.bashrc はユーザ個別の設定ファイル。  
ログイン時に一度だけ設定すればよいものは前者、bashを起動するたびに毎回設定する必要があるものは後者。  
**…だが、基本的には ~/.bashrc に書いておけば問題ない。**  

---
### Chapter09


---
### その他
無操作が続くとログアウト（？）されて再度パスワード入力を求められる件：  
デスクトップ環境で右上から設定⇒プライバシー⇒画面ロック⇒自動画面ロック をOFFにする。


